package elevatorsystem;

public class ScanMovementStrategy implements MovementStrategy {
    @Override
    public void moveElevator(FloorId currentFloor, FloorId destinationFloor) {
        // Logic to move the elevator in a scan pattern
    }
}
